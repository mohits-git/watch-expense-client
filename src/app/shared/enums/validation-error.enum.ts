export enum ValidationError {
  Required = "required",
  Email = "email",
  InvalidPassword = "INVALID_PASSWORD",
  Min = "min",
  Max = "max",
  MinLength = "minlength",
  MaxLength = "maxlength",
  Pattern = "pattern",
}
